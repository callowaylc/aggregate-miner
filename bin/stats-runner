#!/bin/bash
# Exports stats to UDP/8125
set -x

while true; do
  curl -s https://api.coinhive.com/stats/site?secret=${COINHIVE_SECRET} \
      | jq .hashesPerSecond \
      | xargs -I{} \
          echo -n "hashes_per_second:{}|g|#aggregate-miner" >/dev/udp/localhost/8125

  sleep 10
done
